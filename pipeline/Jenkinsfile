pipeline {
  agent any

  stages {
    stage("Build") {
      steps {
        script {
          nodejs('Node-12') {
            print "Build via NODE-12"
            sh "node -v"
          }
        }
      }
    }
  }
}